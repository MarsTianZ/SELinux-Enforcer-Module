# ---------------------------------------------------------------- #
# GENERAL FILES
# ---------------------------------------------------------------- #
# Temporary files generated by the system
*~
*.swp
.DS_Store # macOS
Thumbs.db # Windows

# ---------------------------------------------------------------- #
# ANDROID AND MAGISK/KERNELSU SPECIFIC
# ---------------------------------------------------------------- #
# These files are generated during testing or by the manager app
# and should NOT be uploaded to the repository.

# Log files
*.log

# Debug and temporary files from the Magisk/KSU environment
/system/
/vendor/
/data/
/cache/
/tmp/

# Magisk/KernelSU install/download artifacts
install.zip
module.zip
modulename.zip
debug.zip
disable
.installed_files # Generated by Magisk/KSU after installation

# Exclude compiled files or internal zip structures (if you compile complex mods)
*.o
*.so
*.a
*.apk
*.zip # Exclude compiled release zips (you upload these as a Release, not as code)

# Exclude the directory where you create the final zip for release
# Example if you use a script to build the release zip:
/out/
/build/

# ---------------------------------------------------------------- #
# YOUR LOCAL KERNELSU/MAGISK TEST FOLDER (Optional)
# ---------------------------------------------------------------- #
# If you keep the module source inside a broader working directory
# that might contain local Magisk/KSU test files.
# For example, if you run test scripts that create a directory
# to build the zip.
/SELinux_Enforcer_KSU.zip # If this is the name of your final release zip, ignore it here